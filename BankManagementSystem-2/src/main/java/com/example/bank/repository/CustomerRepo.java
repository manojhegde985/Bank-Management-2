package com.example.bank.repository;
/*
import java.io.Serializable;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.data.repository.NoRepositoryBean;
import org.springframework.stereotype.Repository;

import com.example.bank.BankManagementSystem2Application;
import com.example.bank.entity.Customer;

@NoRepositoryBean
public interface CustomerRepo<T,ID extends Serializable> extends JpaRepository<T, ID>, JpaSpecificationExecutor<T> {

	static final Logger logger = LoggerFactory.getLogger(BankManagementSystem2Application.class);
	default String RepositoryLog() {
	logger.info("this is a bank management repository file ");
	return "RepositoryLog";
	}
}
*/